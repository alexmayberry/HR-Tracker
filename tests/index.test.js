const { isTypedArray } = require("util/types");
const index = require("../index");

describe('add new employee function', () => {
    // arrange
    it('should initialize a new engineer class when engineer is specified as role', () => {

    //act
    addNewEmployee();

    //assert
    expect(hasRun).toBe(true);
    });
})