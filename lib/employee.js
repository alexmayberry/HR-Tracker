// // Import inquirer
// const inquirer = require('inquirer');

// Create an employee class constructor functoin
const Employee = (eName, id, email, role) => {
    this.eName = eName;
    this.id = id;
    this.email = email;
    this.role = role;
}

// getId()

// getEmail()

// getRole()
    // If role = manager ->
    // If role = intern ->
    // If role = engineer ->

//Exports module (obviously XD)
module.exports = Employee;